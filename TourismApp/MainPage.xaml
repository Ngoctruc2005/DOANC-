<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TourismApp.MainPage">

    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">

            <!-- Phần chào -->
            <Image Source="dotnet_bot.png"
                   HeightRequest="185"
                   Aspect="AspectFit" />

            <Label Text="Hello, World!"
                   Style="{StaticResource Headline}" />

            <Label Text="Welcome to .NET Multi-platform App UI"
                   Style="{StaticResource SubHeadline}" />

            <Button x:Name="CounterBtn"
                    Text="Click me"
                    Clicked="OnCounterClicked"
                    HorizontalOptions="Fill" />

            <!-- 👉 Danh sách quán ăn -->
            <Label Text="Quán ăn nổi bật Vĩnh Khánh"
                   FontAttributes="Bold"
                   FontSize="20"
                   TextColor="DarkRed"
                   Margin="0,20,0,10"/>

            <CollectionView x:Name="restaurantList">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BorderColor="LightGray" Padding="10" Margin="0,5">
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="18"/>
                                <Label Text="{Binding Description}" FontSize="14"/>
                                <Label Text="Best: {Binding BestSeller}" TextColor="DarkGreen"/>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
