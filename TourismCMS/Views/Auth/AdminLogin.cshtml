@{
    ViewData["Title"] = "ÄÄƒng Nháº­p Quáº£n Trá»‹";
    Layout = "_Layout";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-5">
        <div class="card shadow-sm border-danger">
            <div class="card-header bg-danger text-white text-center">
                <h4 class="mb-0">ÄÄƒng Nháº­p Há»‡ Thá»‘ng Quáº£n Trá»‹</h4>
            </div>
            <div class="card-body">
                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger">
                        @ViewBag.Error
                    </div>
                }

                <form method="post" asp-controller="Auth" asp-action="AdminLogin">
                    <div class="form-group mb-3">
                        <label class="form-label" for="username">TÃªn Ä‘Äƒng nháº­p / Username Admin</label>
                        <input type="text" id="username" name="username" class="form-control border-danger" required placeholder="Nháº­p tÃªn Ä‘Äƒng nháº­p quáº£n trá»‹" />
                    </div>
                    <div class="form-group mb-4">
                        <label class="form-label" for="password">Máº­t kháº©u / Password</label>
                        <input type="password" id="password" name="password" class="form-control border-danger" required placeholder="Nháº­p máº­t kháº©u" />
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-danger btn-lg">ÄÄƒng Nháº­p (Admin)</button>
                    </div>
                </form>

                <div class="text-center mt-3">
                    <hr />
                    <a href="/Auth/Login" class="text-secondary text-decoration-none">â† Quay láº¡i trang Ä‘Äƒng nháº­p Chá»§ quÃ¡n</a>
                </div>
            </div>
        </div>
    </div>
</div>

