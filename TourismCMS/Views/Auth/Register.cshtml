@{
    ViewData["Title"] = "ÄÄƒng KÃ½ Chá»§ QuÃ¡n";
    Layout = "_Layout";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-5">
        <div class="card shadow-sm">
            <div class="card-header bg-success text-white text-center">
                <h4 class="mb-0">ÄÄƒng KÃ½ TÃ i Khoáº£n Chá»§ QuÃ¡n</h4>
            </div>
            <div class="card-body">
                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger">
                        @ViewBag.Error
                    </div>
                }
                @if (ViewBag.Success != null)
                {
                    <div class="alert alert-success">
                        @ViewBag.Success <br/><a href="/Auth/Login" class="fw-bold">Báº¥m vÃ o Ä‘Ã¢y Ä‘á»ƒ Ä‘Äƒng nháº­p</a>
                    </div>
                }

                <form method="post" asp-controller="Auth" asp-action="Register">
                    <div class="form-group mb-3">
                        <label class="form-label" for="fullName">Há» tÃªn chá»§ quÃ¡n</label>
                        <input type="text" id="fullName" name="fullName" class="form-control" required placeholder="Nháº­p há» tÃªn" />
                    </div>
                    <div class="form-group mb-3">
                        <label class="form-label" for="phoneNumber">Sá»‘ Ä‘iá»‡n thoáº¡i</label>
                        <input type="text" id="phoneNumber" name="phoneNumber" class="form-control" required placeholder="Nháº­p sá»‘ Ä‘iá»‡n thoáº¡i" maxlength="10" minlength="10" pattern="[0-9]{10}" inputmode="numeric" />
                    </div>
                    <div class="form-group mb-3">
                        <label class="form-label" for="username">TÃªn Ä‘Äƒng nháº­p / Username</label>
                        <input type="text" id="username" name="username" class="form-control" required placeholder="Nháº­p tÃªn Ä‘Äƒng nháº­p" />
                    </div>
                    <div class="form-group mb-3">
                        <label class="form-label" for="password">Máº­t kháº©u / Password</label>
                        <input type="password" id="password" name="password" class="form-control" required placeholder="Nháº­p máº­t kháº©u" />
                    </div>
                    <div class="form-group mb-4">
                        <label class="form-label" for="confirmPassword">Nháº­p láº¡i máº­t kháº©u</label>
                        <input type="password" id="confirmPassword" name="confirmPassword" class="form-control" required placeholder="Nháº­p láº¡i máº­t kháº©u" />
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-success btn-lg">ÄÄƒng KÃ½</button>
                    </div>
                </form>
                
                <div class="text-center mt-3">
                    <a href="/Auth/Login" class="text-decoration-none">ÄÃ£ cÃ³ tÃ i khoáº£n? Quay vá» Ä‘Äƒng nháº­p</a>
                </div>
            </div>
        </div>
    </div>
</div>

