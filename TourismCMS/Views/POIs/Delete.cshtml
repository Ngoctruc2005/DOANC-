@model TourismCMS.Models.POI

@{
    ViewData["Title"] = "Xóa quán ăn";
    if (User.IsInRole("admin"))
    {
        ViewData["Layout"] = "~/Views/Shared/_AdminLayout.cshtml";
    }
}

<div class="container mt-4">
    <div class="card shadow-sm border-danger">
            <div class="card-header bg-danger text-white">
    <h4 class="mb-0"><i class="fa fa-exclamation-triangle"></i> Confirm deletion of the restaurant</h4>
        </div>
        <div class="card-body">
            <h5 class="card-title text-danger">Are you sure you want to delete this restaurant?</h5>
            <p class="text-muted">This action will permanently delete the data from the system.</p>
            <hr />

            <dl class="row">
                <dt class="col-sm-3 text-end">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>
                <dd class="col-sm-9 fw-bold text-dark">
                    @Html.DisplayFor(model => model.Name)
                </dd>

                <dt class="col-sm-3 text-end">
                    @Html.DisplayNameFor(model => model.Description)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Description)
                </dd>

                <dt class="col-sm-3 text-end">
                    Tọa độ
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Latitude), @Html.DisplayFor(model => model.Longitude)
                </dd>

                <dt class="col-sm-3 text-end">
                </dt>
                <dd class="col-sm-9">
                </dd>

                @if(!string.IsNullOrEmpty(Model.ImagePath))
                {
                <dt class="col-sm-3 text-end">Image</dt>
                    <dd class="col-sm-9">
                    </dd>
                }

            </dl>
        </div>
        <div class="card-footer text-end bg-light">
            <form asp-action="Delete" onsubmit="return confirm('Xác nhận: Bạn sẽ xóa vĩnh viễn dữ liệu này ra khỏi hệ thống. Tiếp tục?');">
                <input type="hidden" asp-for="Id" />
                <!-- Try binding with Poiid as well if the backend requires it inside the entity, but Id is likely what the delete method expects if it's the route value -->
                <a class="btn btn-secondary me-2" asp-action="Index"><i class="fa fa-arrow-left"></i> Go back</a>
                <button type="submit" class="btn btn-danger"><i class="fa fa-trash"></i> Permanently delete</button>
            </form>
        </div>
    </div>
</div>

