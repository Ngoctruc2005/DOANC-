@model List<POI>

<h2>?? Quán c?a tôi</h2>

<a href="/Owner/Create" class="btn btn-success mb-3">+ Thêm quán</a>

<table class="table table-bordered">
    <tr>
        <th>Tên</th>
        <th>Tr?ng thái</th>
    </tr>

    @foreach (var p in Model)
    {
        <tr>
            <td>@p.Name</td>
            <td>
                @if (p.Status == "pending")
                {
                    <span class="badge bg-warning">Ch? duy?t</span>
                }
                else if (p.Status == "approved")
                {
                    <span class="badge bg-success">Ðã duy?t</span>
                }
                else
                {
                    <span class="badge bg-danger">T? ch?i</span>
                }
            </td>
        </tr>
    }
</table>
