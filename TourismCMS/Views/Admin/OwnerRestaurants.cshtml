@model IEnumerable<TourismCMS.Models.POI>
@{
    ViewData["Title"] = "QuÃ¡n Äƒn cá»§a chá»§ quÃ¡n";
    Layout = "_Layout";
}

<div class="container-fluid mt-4">
    <h2 class="mb-2">QuÃ¡n Äƒn cá»§a: @ViewBag.OwnerName</h2>
    <p class="text-muted">SÄT: @ViewBag.OwnerPhone</p>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover table-striped mb-0">
                <thead class="table-primary">
                    <tr>
                        <th>TÃªn quÃ¡n</th>
                        <th>Äá»‹a chá»‰</th>
                        <th>Tráº¡ng thÃ¡i</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var poi in Model)
                        {
                            var displayStatus = poi.Status == "Ch? duy?t" ? "Chá» duyá»‡t" : poi.Status;
                            <tr>
                                <td>@poi.Name</td>
                                <td>@poi.Address</td>
                                <td>@displayStatus</td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="3" class="text-center py-4 text-muted">
                                <em>Chá»§ quÃ¡n nÃ y chÆ°a cÃ³ quÃ¡n Äƒn nÃ o.</em>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <div class="mt-3">
        <a asp-action="Owners" class="btn btn-secondary">Quay láº¡i danh sÃ¡ch chá»§ quÃ¡n</a>
    </div>
</div>

