@model IEnumerable<TourismCMS.Models.POI>
@{
    ViewData["Title"] = "Danh sÃ¡ch Ä‘Ã£ xÃ³a";
    Layout = "_Layout";
    var deletedOwners = ViewBag.DeletedOwners as IEnumerable<TourismCMS.Models.OwnerListItemViewModel>;
}

<div class="container-fluid mt-4">
    <form method="post" asp-action="PurgeDeleted" onsubmit="return confirm('Báº¡n cÃ³ cháº¯c muá»‘n xÃ³a vÄ©nh viá»…n táº¥t cáº£ dá»¯ liá»‡u Ä‘Ã£ xÃ³a? HÃ nh Ä‘á»™ng nÃ y khÃ´ng thá»ƒ hoÃ n tÃ¡c.');">
        @Html.AntiForgeryToken()
        <button type="submit" class="btn btn-danger mb-3">XÃ³a vÄ©nh viá»…n táº¥t cáº£ dá»¯ liá»‡u Ä‘Ã£ xÃ³a</button>
    </form>
    <h2 class="mb-3">TÃ i khoáº£n chá»§ quÃ¡n Ä‘Ã£ xÃ³a</h2>
    <div class="card shadow-sm mb-4">
        <div class="card-body p-0">
            <table class="table table-hover table-striped mb-0">
                <thead class="table-secondary">
                    <tr>
                        <th>MÃ£ User</th>
                        <th>Há» tÃªn</th>
                        <th>SÄT</th>
                        <th>TÃªn Ä‘Äƒng nháº­p</th>
                        <th>Sá»‘ quÃ¡n</th>
                    </tr>
                </thead>
                <tbody>
                    @if (deletedOwners != null && deletedOwners.Any())
                    {
                        foreach (var owner in deletedOwners)
                        {
                            <tr>
                                <td>@owner.UserId</td>
                                <td>@(string.IsNullOrWhiteSpace(owner.FullName) ? owner.Username : owner.FullName)</td>
                                <td>@owner.PhoneNumber</td>
                                <td>@owner.Username</td>
                                <td>@owner.RestaurantCount</td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center py-4 text-muted"><em>KhÃ´ng cÃ³ tÃ i khoáº£n chá»§ quÃ¡n Ä‘Ã£ xÃ³a.</em></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <h2 class="mb-3">QuÃ¡n Äƒn Ä‘Ã£ xÃ³a </h2>
    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover table-striped mb-0">
                <thead class="table-secondary">
                    <tr>
                        <th>MÃ£ quÃ¡n</th>
                        <th>TÃªn quÃ¡n</th>
                        <th>Äá»‹a chá»‰</th>
                        <th>Tráº¡ng thÃ¡i</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var poi in Model)
                        {
                            <tr>
                                <td>@poi.Poiid</td>
                                <td>@poi.Name</td>
                                <td>@poi.Address</td>
                                <td><span class="badge bg-secondary">@poi.Status</span></td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="4" class="text-center py-4 text-muted"><em>KhÃ´ng cÃ³ quÃ¡n Äƒn Ä‘Ã£ xÃ³a.</em></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

