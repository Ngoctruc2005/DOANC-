@model IEnumerable<TourismCMS.Models.POI>
@{
    ViewData["Title"] = "Danh sÃ¡ch Ä‘Ã£ há»§y";
    Layout = "_Layout";
    var cancelledOwners = ViewBag.CancelledOwners as IEnumerable<TourismCMS.Models.OwnerRegistrationViewModel>;
}

<div class="container-fluid mt-4">
    <h2 class="mb-3">TÃ i khoáº£n chá»§ quÃ¡n Ä‘Ã£ há»§y</h2>
    <div class="card shadow-sm mb-4">
        <div class="card-body p-0">
            <table class="table table-hover table-striped mb-0">
                <thead class="table-danger">
                    <tr>
                        <th>MÃ£ Ä‘Äƒng kÃ½</th>
                        <th>MÃ£ User</th>
                        <th>Há» tÃªn</th>
                        <th>SÄT</th>
                        <th>TÃªn Ä‘Äƒng nháº­p</th>
                        <th>Tráº¡ng thÃ¡i</th>
                    </tr>
                </thead>
                <tbody>
                    @if (cancelledOwners != null && cancelledOwners.Any())
                    {
                        foreach (var owner in cancelledOwners)
                        {
                            <tr>
                                <td>@owner.RegistrationId</td>
                                <td>@owner.UserId</td>
                                <td>@owner.FullName</td>
                                <td>@owner.PhoneNumber</td>
                                <td>@owner.Username</td>
                                <td><span class="badge bg-danger">ÄÃ£ há»§y</span></td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6" class="text-center py-4 text-muted"><em>KhÃ´ng cÃ³ tÃ i khoáº£n chá»§ quÃ¡n Ä‘Ã£ há»§y.</em></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <h2 class="mb-3">QuÃ¡n Äƒn Ä‘Ã£ há»§y</h2>
    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover table-striped mb-0">
                <thead class="table-danger">
                    <tr>
                        <th>MÃ£ quÃ¡n</th>
                        <th>TÃªn quÃ¡n</th>
                        <th>Äá»‹a chá»‰</th>
                        <th>Tráº¡ng thÃ¡i</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var poi in Model)
                        {
                            <tr>
                                <td>@poi.Poiid</td>
                                <td>@poi.Name</td>
                                <td>@poi.Address</td>
                                <td><span class="badge bg-danger">@poi.Status</span></td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="4" class="text-center py-4 text-muted"><em>KhÃ´ng cÃ³ quÃ¡n Äƒn Ä‘Ã£ há»§y.</em></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

