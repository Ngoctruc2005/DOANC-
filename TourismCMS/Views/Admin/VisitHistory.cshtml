@model List<TourismCMS.Models.VisitHistoryItemViewModel>

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Lá»‹ch sá»­ sá»­ dá»¥ng";
    ViewData["PageHeaderIcon"] = "ðŸ“ˆ";
    ViewData["PageHeaderTitle"] = "Lá»‹ch sá»­ sá»­ dá»¥ng";
    ViewData["PageHeaderSubtitle"] = "Thá»‘ng kÃª lÆ°á»£t truy cáº­p cÃ¡c quÃ¡n Äƒn";

    var totalVisits = Model.Sum(x => x.TotalVisits);
    var totalRestaurants = Model.Count;
}

@await Html.PartialAsync("_AdminPageHeader")

<div class="row g-3 mb-3">
    <div class="col-md-4">
        <div class="card border-0 shadow-sm p-3 text-center">
            <h6>Tá»•ng lÆ°á»£t truy cáº­p</h6>
            <h3 class="text-primary">@totalVisits</h3>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card border-0 shadow-sm p-3 text-center">
            <h6>Sá»‘ quÃ¡n cÃ³ truy cáº­p</h6>
            <h3 class="text-success">@totalRestaurants</h3>
        </div>
    </div>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead>
                    <tr>
                        <th>QuÃ¡n Äƒn</th>
                        <th>Chá»§ quÃ¡n</th>
                        <th>Äá»‹a chá»‰</th>
                        <th>LÆ°á»£t truy cáº­p</th>
                        <th>Láº§n truy cáº­p gáº§n nháº¥t</th>
                    </tr>
                </thead>
                <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="5" class="text-center text-muted">ChÆ°a cÃ³ dá»¯ liá»‡u lá»‹ch sá»­ sá»­ dá»¥ng.</td>
                    </tr>
                }
                else
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <a asp-controller="POIs" asp-action="Details" asp-route-id="@item.Poiid">@item.PoiName</a>
                            </td>
                            <td>@(string.IsNullOrWhiteSpace(item.OwnerName) ? "-" : item.OwnerName)</td>
                            <td>@item.Address</td>
                            <td><span class="badge bg-primary">@item.TotalVisits</span></td>
                            <td>@(item.LastVisitTime?.ToString("dd/MM/yyyy HH:mm") ?? "-")</td>
                        </tr>
                    }
                }
                </tbody>
            </table>
        </div>
    </div>
</div>

