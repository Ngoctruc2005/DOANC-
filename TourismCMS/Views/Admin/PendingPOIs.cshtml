@model List<POI>

<h2>? Duy?t quán</h2>

<table class="table">
    <tr>
        <th>Tên</th>
        <th>Hành d?ng</th>
    </tr>

    @foreach (var p in Model)
    {
        <tr>
            <td>@p.Name</td>
            <td>
                <a href="/Admin/Approve/@p.Id" class="btn btn-success btn-sm">? Duy?t</a>
                <a href="/Admin/Reject/@p.Id" class="btn btn-danger btn-sm">? T? ch?i</a>
            </td>
        </tr>
    }
</table>
